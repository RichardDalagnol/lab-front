import styled from 'styled-components';

export const Container = styled.div`
  button {
    background: transparent;
    margin-right: 8px;
    border: none;
  }

  svg {
    height: 1.2em;
    width: 1.2em;
  }
`;
